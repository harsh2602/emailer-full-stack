import React from 'react';

const Landing = () => {
  return (
    <div style={styles}>
      <h1>Emailer!</h1>
      Collect feedback from users
    </div>
  );
};

const styles = {
  textAlign: 'center'
};

export default Landing;
